import { PagePlaceholder } from "@/components/ui/page-placeholder";
export default function Page() {
  return <PagePlaceholder eyebrow="admin / people / batches" title="Batches" description="Group learners into batches and assign supervisors." />;
}
