import { PagePlaceholder } from "@/components/ui/page-placeholder";
export default function Page() {
  return <PagePlaceholder eyebrow="admin / people / users" title="Users" description="Every user with roles, batches, progress and streaks — promote, disable, or inspect activity." />;
}
