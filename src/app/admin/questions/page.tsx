import { PagePlaceholder } from "@/components/ui/page-placeholder";
export default function Page() {
  return <PagePlaceholder eyebrow="admin / content / questions" title="Question bank" description="Create and manage questions, map them to roadmap steps, and add unlimited test cases (CSV bulk import or one at a time)." />;
}
