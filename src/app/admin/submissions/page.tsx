import { PagePlaceholder } from "@/components/ui/page-placeholder";
export default function Page() {
  return <PagePlaceholder eyebrow="admin / activity / submissions" title="Submissions monitor" description="Live feed of submissions across the platform with verdicts, runtime/memory, and Judge0 tokens." />;
}
