import { PagePlaceholder } from "@/components/ui/page-placeholder";
export default function Page() {
  return <PagePlaceholder eyebrow="admin / content / roadmap" title="Tracks & roadmap builder" description="Build tracks, modules, and lessons; order steps and set unlock rules." />;
}
