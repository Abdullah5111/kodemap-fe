import { PagePlaceholder } from "@/components/ui/page-placeholder";
export default function Page() {
  return <PagePlaceholder eyebrow="admin / overview" title="Platform overview" description="KPIs, submission trends, verdict distribution, and recent activity across the platform." />;
}
