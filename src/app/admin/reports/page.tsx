import { PagePlaceholder } from "@/components/ui/page-placeholder";
export default function Page() {
  return <PagePlaceholder eyebrow="admin / activity / reports" title="Reports" description="Supervisor progress reports: batch performance, weak topics, and weekly activity." />;
}
