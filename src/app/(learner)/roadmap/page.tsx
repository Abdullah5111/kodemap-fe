import { PagePlaceholder } from "@/components/ui/page-placeholder";
export default function Page() {
  return <PagePlaceholder eyebrow="tracks / dsa foundation" title="Roadmap" description="Follow the path: tracks break into modules and lessons, each with must-do questions. Clear a step to unlock the next." />;
}
