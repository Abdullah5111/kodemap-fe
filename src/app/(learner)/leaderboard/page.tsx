import { PagePlaceholder } from "@/components/ui/page-placeholder";
export default function Page() {
  return <PagePlaceholder eyebrow="learn / leaderboard" title="Leaderboard" description="Global, batch, and weekly rankings by score and solved count — with daily streaks." />;
}
